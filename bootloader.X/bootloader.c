#define CO_FCY     40000      /* (8MHz Quartz used) */

#include "CANopen.h"


/* Global variables and objects */
const CO_CANbitRateData_t  CO_CANbitRateData[8] = {CO_CANbitRateDataInitializers};

int main()
{
    while(1) {
    }
    
    return 0;
}